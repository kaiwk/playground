#include <iostream>

using namespace std;

int T, N;

int main() {

    int T, N;

    while (T >= 0) {
        T--;
        cin >> N;
        int p[0] = {0};
        for (int i = 0; i < N; i++) {
        }
    }


    return 0;
}
